# backend/requirements.txt
flask==2.2.5
flask-cors==3.0.10
pillow==9.5.0
numpy==1.25.3

# CPU-only TensorFlow (more reliable on Render)
tensorflow-cpu==2.12.0

# mediapipe (may still be tricky; we try a known release)
mediapipe==0.10.2

# server
gunicorn==20.1.0
requests==2.31.0
